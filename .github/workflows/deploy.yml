name: Auto Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Trigger Render Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Send Deploy Request to Render
        run: |
          curl -s -X POST "https://api.render.com/deploy/srv-daoq7hbtqb8s73e60log?key=fdTu8R5hqjA"
