{
  "buildCommand": "cd viewer-ui && npm run build",
  "outputDirectory": "viewer-ui/dist",
  "installCommand": "cd viewer-ui && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
